import { DisplayRangePipe } from './display-range.pipe';

describe('DisplayRangePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRangePipe();
    expect(pipe).toBeTruthy();
  });
});
